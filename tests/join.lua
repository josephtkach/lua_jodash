-------------------------------------------------------------------------------
local test = junit:new({ name = "Array:Join" })

-------------------------------------------------------------------------------
function test.Default(data)
    expect( jo.join({'a', "A", false, 1}) )
        :toBe( "a,A,false,1" )
end

-------------------------------------------------------------------------------
function test.Empty(data)
    expect( jo.join( data.empty ) )
        :toBe( "" )
end

-------------------------------------------------------------------------------
function test.Tables(data)
    expect( jo.join({{foo="bar"}}) )
        :toBe( "{foo=bar}" )
end

-------------------------------------------------------------------------------
return test
