prettify output on unit tests
automatically read unit tests from test library?

-- onlyfailed command line option

init function
    -- string metatable options

    --supend lobal metatable if it exists
    --remove _G[__] and restore it at the end
